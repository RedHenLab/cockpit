const url = 'http://localhost:4000';

const 
    list = `${url}/list`,
    add = `${url}/add`,
    edit =  `${url}/edit`,
    remove = `${url}/delete`,
    refresh = `${url}/refresh`,
    report = `${url}/report`,
    login = `${url}/users/login`;

export {list, add, edit, remove, refresh, report, login };